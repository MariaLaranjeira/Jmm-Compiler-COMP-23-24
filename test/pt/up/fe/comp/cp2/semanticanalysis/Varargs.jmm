class Varargs {

    public int foo() {
        int a;
        a = varargs(10, 20, 30);
        return a;
    }

    // the varargs here
    public int varargs(int... a) {
        return a[0];
    }
}